# NetworkSlimming-Replication
A PyTorch-based replication of Network Slimming, implementing channel-level pruning using batch normalization scaling factors. The repository demonstrates a simple approach to compress CNNs while maintaining accuracy.
